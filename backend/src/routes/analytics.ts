import express from 'express';

const router = express.Router();

// Placeholder para rutas de analytics
router.get('/', async (req: any, res: any) => {
  res.json({ message: 'Analytics routes coming soon' });
});

export default router;
